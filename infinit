<!DOCTYPE html>
<html lang="el">
<head>
  <meta charset="UTF-8">
  <title>∞ Power Clicker 2^n</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      background: #000;
      color: #0f0;
      font-family: monospace;
      text-align: center;
      padding: 30px;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 20px;
    }

    #display {
      font-size: 1.6rem;
      margin: 20px 0;
    }

    button, select {
      background: #111;
      color: #0f0;
      border: 1px solid #0f0;
      padding: 10px 20px;
      font-size: 1rem;
      margin: 5px;
      cursor: pointer;
    }

    button:hover, select:hover {
      background-color: #0f0;
      color: #000;
    }

    footer {
      margin-top: 40px;
      font-size: 0.8rem;
      color: #555;
    }
  </style>
</head>
<body>

  <h1>∞ AUTO POWER CLICKER</h1>
  <div id="display">2^0</div>

  <div>
    <button onclick="clickOnce()">CLICK</button>
    <button onclick="reset()">RESET</button>
    <button onclick="toggleAutoClick()">
      <span id="autoBtnLabel">START AUTO</span>
    </button>
  </div>

  <div>
    <label for="speedSelect">Speed:</label>
    <select id="speedSelect">
      <option value="100">1^2 (×100)</option>
      <option value="1000">1^3 (×1,000)</option>
      <option value="10000">1^4 (×10,000)</option>
      <option value="100000">1^5 (×100,000)</option>
      <option value="1000000">1^6 (×1,000,000)</option>
    </select>
  </div>

  <footer>
    Ταχύτητα ως 1^n & Διπλασιασμός 2^n χωρίς όριο 🚀
  </footer>

  <script>
    let power = 0n;
    let autoRunning = false;
    let frame;

    const display = document.getElementById("display");
    const autoBtnLabel = document.getElementById("autoBtnLabel");
    const speedSelect = document.getElementById("speedSelect");

    function updateDisplay() {
      display.innerText = "2^" + power.toString();
    }

    function clickOnce() {
      power += 1n;
      updateDisplay();
    }

    function reset() {
      power = 0n;
      autoRunning = false;
      cancelAnimationFrame(frame);
      autoBtnLabel.textContent = "START AUTO";
      updateDisplay();
    }

    function toggleAutoClick() {
      autoRunning = !autoRunning;
      autoBtnLabel.textContent = autoRunning ? "STOP AUTO" : "START AUTO";
      if (autoRunning) {
        autoClickLoop();
      } else {
        cancelAnimationFrame(frame);
      }
    }

    function autoClickLoop() {
      if (!autoRunning) return;
      const speed = parseInt(speedSelect.value);
      power += BigInt(speed);
      updateDisplay();
      frame = requestAnimationFrame(autoClickLoop);
    }

    updateDisplay();
  </script>

</body>
</html>
