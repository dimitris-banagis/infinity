<!DOCTYPE html>
<html lang="el">
<head>
  <meta charset="UTF-8">
  <title>∞ Auto Multiplier Clicker</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      background-color: #000;
      color: #0f0;
      font-family: 'Courier New', monospace;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
    }

    h1 {
      font-size: 2.2rem;
      margin-bottom: 20px;
    }

    #numberDisplay {
      font-size: 1.5rem;
      margin: 20px 0;
      word-break: break-word;
      max-width: 90%;
      text-align: center;
    }

    .buttons, .controls {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 10px;
      margin: 10px 0;
    }

    button, select {
      padding: 10px 20px;
      background-color: #111;
      color: #0f0;
      border: 1px solid #0f0;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.2s, color 0.2s;
    }

    button:hover, select:hover {
      background-color: #0f0;
      color: #000;
    }

    label {
      font-size: 1rem;
    }

    footer {
      margin-top: 30px;
      font-size: 0.8rem;
      color: #444;
    }
  </style>
</head>
<body>

  <h1>∞ AUTO MULTIPLIER CLICKER</h1>
  <div id="numberDisplay">1</div>

  <div class="buttons">
    <button onclick="manualClick()">CLICK</button>
    <button onclick="doubleNow()">x2</button>
    <button onclick="reset()">RESET</button>
    <button onclick="toggleAutoClick()">
      <span id="autoBtnLabel">START AUTO</span>
    </button>
  </div>

  <div class="controls">
    <label for="speedSelect">Speed:</label>
    <select id="speedSelect">
      <option value="100">Ultra Fast ×100</option>
      <option value="1000">Mega Fast ×1,000</option>
      <option value="5000">Super Fast ×5,000</option>
      <option value="10000">Hyper ×10,000</option>
      <option value="100000">Insane ×100,000</option>
      <option value="1000000">Overdrive ×1,000,000</option>
    </select>
  </div>

  <footer>
    Infinite clicker | χωρίς όρια | φτιαγμένο με BigInt 🚀
  </footer>

  <script>
    let number = 1n;
    let autoRunning = false;
    let frame;

    const display = document.getElementById("numberDisplay");
    const autoBtnLabel = document.getElementById("autoBtnLabel");
    const speedSelect = document.getElementById("speedSelect");

    function updateDisplay() {
      display.innerText = number.toString();
    }

    function manualClick() {
      number *= 2n;
      updateDisplay();
    }

    function doubleNow() {
      number *= 2n;
      updateDisplay();
    }

    function reset() {
      number = 1n;
      autoRunning = false;
      cancelAnimationFrame(frame);
      autoBtnLabel.textContent = "START AUTO";
      updateDisplay();
    }

    function toggleAutoClick() {
      autoRunning = !autoRunning;
      autoBtnLabel.textContent = autoRunning ? "STOP AUTO" : "START AUTO";
      if (autoRunning) {
        autoClickLoop();
      } else {
        cancelAnimationFrame(frame);
      }
    }

    function autoClickLoop() {
      if (!autoRunning) return;
      const speed = parseInt(speedSelect.value);
      for (let i = 0; i < speed; i++) {
        number *= 2n;
      }
      updateDisplay();
      frame = requestAnimationFrame(autoClickLoop);
    }

    updateDisplay();
  </script>

</body>
</html>
